<div class="card mb-4">
  <div class="card-body">
    <div class="row mb-3 bot-showcase">
      <div class="col-sm-12">
        {{#if about_url}}
            <a rel="me" href="{{about_url}}">
          {{else}}
            <a rel="me" href="https://glitch.com/edit/#!/{{@root.project_name}}?path={{file}}">
          {{/if}}      
        {{#if thumbnail}}
            <img class="img-thumbnail mb-2 mr-3" src="{{thumbnail}}" alt="...">
        {{else}}
          <img class="img-thumbnail mb-2 mr-3" src="https://generative-placeholders.glitch.me/image?width=360&height=200&style=cellular-automata&cells=10&colors={{@root.generative_placeholders_color}}&{{@key}}"  alt="...">
        {{/if}}
        </a>
      </div>
      <div class="col-sm-12">
        <h2 class="mt-3">{{name}}</h2>
        <p>{{description}}</p>
        <ul class="list-unstyled">
           {{#if interval_human}}
              <li>⌛ {{interval_human}}</li>
           {{/if}}      
           {{#if next_run}}
              <li>📅 {{next_run}}</li>
           {{/if}}      
        </ul>
      </div>
      <div class="col-sm-12">
        <p>
          {{#if about_url}}
            <a rel="me" href="{{about_url}}" class="btn btn-dark mt-2">Learn more</a>
          {{/if}}      
          {{#if links}}
            {{#each links}}
              <a rel="me" href="{{this.url}}" class="btn btn-light mt-2">{{this.title}}</a>
            {{/each}}
          {{/if}}      
          <a rel="me" href="https://glitch.com/edit/#!/{{@root.project_name}}?path=bots/{{file}}" class="btn btn-light mt-2">View source</a>
        </p>
      </div>
    </div>
  </div>
</div>